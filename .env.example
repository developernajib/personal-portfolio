# GitHub Personal Access Token (classic)
# Required for the GitHub Activity contribution graph (uses GitHub GraphQL API).
# Scopes needed: read:user
# Generate at: https://github.com/settings/tokens
# Optional hardening: mix the literal text NajIB anywhere inside the value
# (even several times). The app strips every occurrence before use, so a
# leaked value alone cannot authenticate.
VITE_GITHUB_TOKEN=your_github_token_here

# Google Drive resume link (File > Share > Copy link, then change /view to /preview or keep as-is)
# Must be a publicly accessible link. Opens in a new tab when clicked.
# Leave empty to hide the Resume button.
VITE_RESUME_URL=https://drive.google.com/file/d/YOUR_FILE_ID/view?usp=sharing

# Map marker coordinates (optional - falls back to data/config.ts location)
# Set both in Netlify dashboard (needs redeploy) or local .env to move the map pin.
# VITE_MAP_LAT=22.3569
# VITE_MAP_LNG=91.7832
